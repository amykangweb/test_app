<%= form_for(@peep) do |f| %>
  <% if @peep.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@peep.errors.count, "error") %> prohibited this peep from being saved:</h2>

      <ul>
      <% @peep.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :content %><br>
    <%= f.text_field :content %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
